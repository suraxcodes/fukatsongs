---
phase: 11-playlist-library
task: 2
total_tasks: 3
status: in_progress
last_updated: 2026-05-15T21:35:24.798Z
---

<current_state>
We are paused after debugging audio engine race conditions and YouTube 403 playback errors. The user requested to revert the `youtube_provider.dart` fallback block back to the original `api.piped.private.coffee` proxy URL. The app is currently waiting to be hot restarted and tested.
</current_state>

<completed_work>

- Task 1: Implemented Untyped Hive Box persistence for Playlist and Liked Songs - Done
- Task 2: Fixed Audio Race conditions with session IDs in `MusicAudioHandler` and `PlayerNotifier` - Done
</completed_work>

<remaining_work>

- Task 3: Verify that YouTube streams are working again with the reverted code.
</remaining_work>

<decisions_made>

- Decided to use `_playSessionId` in `MusicAudioHandler` because it cleanly cancels playback of delayed URL fetches when the user skips tracks.
- Decided to revert `youtube_provider.dart` to use `api.piped.private.coffee` instead of `youtube_explode_dart` because the direct fetches were causing `403 Forbidden` errors for the user in `just_audio`.
</decisions_made>

<blockers>
- Piped Tunnels are extremely unstable today (DNS failures and 403s). If the reverted code doesn't work, we may need to build a custom proxy or fix the `User-Agent` headers more robustly.
</blockers>

<context>
The goal of this session was to stabilize the playlist and queue engine. We successfully built a robust queue in `PlayerNotifier`. However, during testing, overlapping audio bugs and YouTube API failures surfaced, which we addressed.
</context>

<next_action>
Start with: Ask the user if the reverted YouTube playback code worked after a hot restart, and if not, investigate the Dio 403 logs on the Piped proxy.
</next_action>
